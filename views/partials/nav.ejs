<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm py-3">
  <div class="container-fluid">
    <!-- Navbar Brand -->
    <a class="navbar-brand text-light fw-bold fs-4" href="/">Blogify</a>

    <!-- Toggle Button for Mobile -->
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
      aria-controls="navbarNav"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Navbar Content -->
    <div class="collapse navbar-collapse" id="navbarNav">
      <!-- Navigation Links -->
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link text-light fw-semibold hover-underline" href="/"
            >Home</a
          >
        </li>
        <% if (user) { %>
        <li class="nav-item">
          <a
            class="nav-link text-light fw-semibold hover-underline"
            href="/blog/add-new"
            >Add Blog</a
          >
        </li>
        <% } %>
      </ul>

      <!-- User Info and Authentication Links -->
      <ul class="navbar-nav ms-auto align-items-center">
        <% if (user) { %>
        <li class="nav-item me-3">
          <span class="navbar-text text-light">
            Welcome, <strong class="text-primary"><%= user.fullName %></strong>
          </span>
        </li>
        <li class="nav-item me-3">
          <!-- Display Profile Image -->
          <img
            src="<%= user.profileImageURL || '/images/default-profile.png' %>"
            alt="Profile Image"
            class="rounded-circle"
            style="width: 40px; height: 40px; object-fit: cover"
          />
        </li>
        <li class="nav-item">
          <a class="btn btn-outline-light btn-sm px-3" href="/user/logout"
            >Sign Out</a
          >
        </li>
        <% } else { %>
        <li class="nav-item me-2">
          <a class="btn btn-outline-primary btn-sm px-3" href="/user/signin"
            >Sign In</a
          >
        </li>
        <li class="nav-item">
          <a class="btn btn-primary btn-sm px-3" href="/user/signup"
            >Create Account</a
          >
        </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
